# Optional — coach LLM (otherwise built-in mock replies)
# Prefer NVIDIA NIM (Gemma 4, etc.): https://build.nvidia.com/
NVIDIA_API_KEY=
# NVIDIA_MODEL=google/gemma-4-31b-it
# NVIDIA_MAX_TOKENS=3072
# NVIDIA_TEMPERATURE=0.55
# NVIDIA_TOP_P=0.92
# Set true only if you accept much slower responses (hidden reasoning phase)
# NVIDIA_ENABLE_THINKING=false

# Fallback if NVIDIA_API_KEY is unset
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Optional — cloud sync for study plan API only (UI uses localStorage by default)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
