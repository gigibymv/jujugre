/** Short fallback when the coach API fails or returns nothing (no demo transcripts). */
export const COACH_CLIENT_FALLBACK =
  'The coach could not return a reply. Check your connection or API keys in settings, then try again.';
